import React from "react";
import { Typography } from "antd";

export const PageHeader: React.FC = () => (
  <Typography.Title level={2}>
    Рекомендательная система для библиотеки
  </Typography.Title>
);
